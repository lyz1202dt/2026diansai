# 项目引脚使用整理

本文档根据 `empty.syscfg`、`Debug/syscfg/ti_msp_dl_config.h` 以及当前业务代码整理，适用于当前工程的 MSPM0G3507 LQFP-64 配置。

## 说明

- “端口引脚”采用 MCU GPIO 名称，例如 `PA12`、`PB4`。
- “封装脚号”来自 SysConfig 生成头文件注释。
- “当前使用情况”区分为：
  - `代码直接使用`：当前源码中有运行时调用或初始化依赖。
  - `SysConfig 已初始化`：系统初始化会配置该引脚，但当前业务代码未直接调用，可能为保留或后续功能。
  - `代码中已注释`：源码中有相关逻辑，但目前被注释掉。

## 运动控制

### L298N 电机方向控制

来源：`APP/config.c` 中 `g_l298n`，`Bsp/motor.c` 中 `L298N_SetPWMValue()`。


| 功能          | SysConfig 名称  | 端口引脚 | 封装脚号 | 方向/模式 | 当前使用情况 |
| ------------- | --------------- | -------- | -------- | --------- | ------------ |
| 电机 1 方向 A | `MOTOR_PIN_M1A` | `PA12`   | 5        | GPIO 输出 | 代码直接使用 |
| 电机 1 方向 B | `MOTOR_PIN_M1B` | `PA13`   | 6        | GPIO 输出 | 代码直接使用 |
| 电机 2 方向 A | `MOTOR_PIN_M2A` | `PA14`   | 7        | GPIO 输出 | 代码直接使用 |
| 电机 2 方向 B | `MOTOR_PIN_M2B` | `PA16`   | 9        | GPIO 输出 | 代码直接使用 |

### 车轮 PWM

来源：`APP/config.c` 中 `WHEEL_PWM_INST` 与 `GPIO_WHEEL_PWM_C0_IDX/C1_IDX`，`Bsp/motor.c` 中 `DL_Timer_setCaptureCompareValue()`。


| 功能       | 外设/通道    | 端口引脚 | 封装脚号 | 方向/模式       | 当前使用情况 |
| ---------- | ------------ | -------- | -------- | --------------- | ------------ |
| 电机 1 PWM | `TIMA1_CCP0` | `PB4`    | 52       | 定时器 PWM 输出 | 代码直接使用 |
| 电机 2 PWM | `TIMA1_CCP1` | `PB5`    | 53       | 定时器 PWM 输出 | 代码直接使用 |

### 编码器输入

来源：`APP/config.c` 中 `g_encoder1/g_encoder2`，`APP/irq.c` 中 `GROUP1_IRQHandler()`。


| 功能          | SysConfig 名称    | 端口引脚 | 封装脚号 | 方向/模式             | 当前使用情况 |
| ------------- | ----------------- | -------- | -------- | --------------------- | ------------ |
| 编码器 1 A 相 | `ENCODER_PIN_E1A` | `PB3`    | 51       | GPIO 输入，双边沿中断 | 代码直接使用 |
| 编码器 1 B 相 | `ENCODER_PIN_E1B` | `PB2`    | 50       | GPIO 输入，双边沿中断 | 代码直接使用 |
| 编码器 2 A 相 | `ENCODER_PIN_E2A` | `PA7`    | 49       | GPIO 输入，双边沿中断 | 代码直接使用 |
| 编码器 2 B 相 | `ENCODER_PIN_E2B` | `PA22`   | 18       | GPIO 输入，双边沿中断 | 代码直接使用 |

## 传感器与采集

### MPU6050 I2C

来源：`Bsp/mpu6050.c` 中 `MPU6050_Init()` 与 I2C 读写函数。


| 功能        | 外设信号   | 端口引脚 | 封装脚号/PinCM | 方向/模式    | 当前使用情况 |
| ----------- | ---------- | -------- | -------------- | ------------ | ------------ |
| MPU6050 SDA | `I2C1_SDA` | `PA30`   | `IOMUX_PINCM5` | I2C，400 kHz | 代码直接使用 |
| MPU6050 SCL | `I2C1_SCL` | `PA29`   | `IOMUX_PINCM4` | I2C，400 kHz | 代码直接使用 |

备注：代码中包含 I2C 总线恢复逻辑，会临时把 SCL/SDA 切为 GPIO 模式。

### 循迹/灰度模块

来源：`Bsp/gw_model.c` 中 `GWGetState()` 和 `switch_sensor_channel()`。


| 功能       | SysConfig 名称      | 端口引脚 | 封装脚号 | 方向/模式       | 当前使用情况 |
| ---------- | ------------------- | -------- | -------- | --------------- | ------------ |
| 模拟量输入 | `ADC12_0_C5`        | `PB18`   | 15       | ADC1 输入通道 5 | 代码直接使用 |
| 地址选择 0 | `GW_ADDR_PIN_ADDR0` | `PB7`    | 59       | GPIO 输出       | 代码直接使用 |
| 地址选择 1 | `GW_ADDR_PIN_ADDR1` | `PB15`   | 3        | GPIO 输出       | 代码直接使用 |
| 地址选择 2 | `GW_ADDR_PIN_ADDR2` | `PB25`   | 27       | GPIO 输出       | 代码直接使用 |

## 通信接口

### 当前业务串口

来源：`APP/config.c` 中 `SerialInit(UART_0_INST, ...)`、`SerialInit(UART_1_INST, ...)`，`APP/irq.c` 中 UART 中断服务函数。


| 功能     | 外设信号   | 端口引脚 | 封装脚号/PinCM  | 波特率 | 当前使用情况 |
| -------- | ---------- | -------- | --------------- | ------ | ------------ |
| UART0 TX | `UART0_TX` | `PB0`    | `IOMUX_PINCM12` | 115200 | 代码直接使用 |
| UART0 RX | `UART0_RX` | `PB1`    | `IOMUX_PINCM13` | 115200 | 代码直接使用 |
| UART1 TX | `UART1_TX` | `PA8`    | `IOMUX_PINCM19` | 115200 | 代码直接使用 |
| UART1 RX | `UART1_RX` | `PA9`    | `IOMUX_PINCM20` | 115200 | 代码直接使用 |

备注：`UART0` 对应全局 `g_serial`，`UART1` 对应 `zdt_serial`，后者供 ZDT 驱动通信封装使用。

### 已配置但当前未直接使用的串口


| 功能     | 外设信号   | 端口引脚 | 封装脚号/PinCM  | 波特率 | 当前使用情况       |
| -------- | ---------- | -------- | --------------- | ------ | ------------------ |
| UART2 TX | `UART2_TX` | `PA21`   | `IOMUX_PINCM46` | 115200 | SysConfig 已初始化 |
| UART2 RX | `UART2_RX` | `PA24`   | `IOMUX_PINCM54` | 115200 | SysConfig 已初始化 |
| UART3 TX | `UART3_TX` | `PA26`   | `IOMUX_PINCM59` | 115200 | SysConfig 已初始化 |
| UART3 RX | `UART3_RX` | `PA25`   | `IOMUX_PINCM55` | 115200 | SysConfig 已初始化 |

## 显示与人机接口

### OLED I2C

来源：`Bsp/OLED.c`。当前 `app_main.c` 中 `OLED_Init()` 被注释，但 OLED 驱动本身使用以下 I2C 引脚。


| 功能     | 外设信号   | 端口引脚 | 封装脚号/PinCM | 方向/模式    | 当前使用情况               |
| -------- | ---------- | -------- | -------------- | ------------ | -------------------------- |
| OLED SDA | `I2C0_SDA` | `PA28`   | `IOMUX_PINCM3` | I2C，400 kHz | 驱动代码使用，入口当前注释 |
| OLED SCL | `I2C0_SCL` | `PA31`   | `IOMUX_PINCM6` | I2C，400 kHz | 驱动代码使用，入口当前注释 |

备注：OLED I2C 驱动也包含总线恢复逻辑，会临时把 SCL/SDA 切为 GPIO 模式。

### 按键

来源：`app_main.c` 中按键读取逻辑，目前整段被注释。


| 功能     | SysConfig 名称 | 端口引脚 | 封装脚号 | 方向/模式       | 当前使用情况 |
| -------- | -------------- | -------- | -------- | --------------- | ------------ |
| 三键 K1  | `KEY3_K1`      | `PB21`   | 20       | GPIO 输入，上拉 | 代码中已注释 |
| 三键 K2  | `KEY3_K2`      | `PB23`   | 22       | GPIO 输入，上拉 | 代码中已注释 |
| 三键 K3  | `KEY3_K3`      | `PB24`   | 23       | GPIO 输入，上拉 | 代码中已注释 |
| 五向键上 | `KEY5_K_UP`    | `PB12`   | 64       | GPIO 输入，上拉 | 代码中已注释 |
| 五向键下 | `KEY5_K_DOWN`  | `PB8`    | 60       | GPIO 输入，上拉 | 代码中已注释 |
| 五向键左 | `KEY5_K_LEFT`  | `PB9`    | 61       | GPIO 输入，上拉 | 代码中已注释 |
| 五向键右 | `KEY5_K_RIGHT` | `PB10`   | 62       | GPIO 输入，上拉 | 代码中已注释 |
| 五向键中 | `KEY5_K_MAIN`  | `PB11`   | 63       | GPIO 输入，上拉 | 代码中已注释 |

### LED 与蜂鸣器

来源：SysConfig GPIO 初始化。当前业务代码未直接操作这些引脚。


| 功能     | SysConfig 名称  | 端口引脚 | 封装脚号 | 方向/模式 | 当前使用情况       |
| -------- | --------------- | -------- | -------- | --------- | ------------------ |
| 单色 LED | `LED_LED0_PIN`  | `PA15`   | 8        | GPIO 输出 | SysConfig 已初始化 |
| 蜂鸣器   | `BEEP_BEEP_PIN` | `PA27`   | 31       | GPIO 输出 | SysConfig 已初始化 |
| RGB 红灯 | `LED_RGB_LED_R` | `PB26`   | 28       | GPIO 输出 | SysConfig 已初始化 |
| RGB 绿灯 | `LED_RGB_LED_G` | `PB27`   | 29       | GPIO 输出 | SysConfig 已初始化 |
| RGB 蓝灯 | `LED_RGB_LED_B` | `PB22`   | 21       | GPIO 输出 | SysConfig 已初始化 |

## 已配置/保留外设引脚

以下引脚由 SysConfig 生成代码初始化，但当前业务源码没有直接调用其外设实例或控制脚。

### 额外 PWM


| 功能         | 外设信号     | 端口引脚 | 封装脚号/PinCM | 方向/模式 | 当前使用情况       |
| ------------ | ------------ | -------- | -------------- | --------- | ------------------ |
| PWM_0 通道 0 | `TIMG7_CCP0` | `PA3`    | `IOMUX_PINCM8` | PWM 输出  | SysConfig 已初始化 |
| PWM_0 通道 1 | `TIMG7_CCP1` | `PA4`    | `IOMUX_PINCM9` | PWM 输出  | SysConfig 已初始化 |

## 调试接口

来源：`empty.syscfg` 中 Board 调试接口配置。


| 功能  | 端口引脚 | 当前使用情况                 |
| ----- | -------- | ---------------------------- |
| SWDIO | `PA19`   | 调试接口                     |
| SWCLK | `PA20`   | 调试接口，SysConfig 建议方案 |

## 按端口汇总

### PORTA


| 端口引脚 | 分类        | 功能               |
| -------- | ----------- | ------------------ |
| `PA2`    | 已配置/保留 | OLED SPI CS        |
| `PA3`    | 已配置/保留 | PWM_0 通道 0       |
| `PA4`    | 已配置/保留 | PWM_0 通道 1       |
| `PA7`    | 运动控制    | 编码器 2 A 相      |
| `PA8`    | 通信        | UART1 TX           |
| `PA9`    | 通信        | UART1 RX           |
| `PA12`   | 运动控制    | 电机 1 方向 A      |
| `PA13`   | 运动控制    | 电机 1 方向 B      |
| `PA14`   | 运动控制    | 电机 2 方向 A      |
| `PA15`   | 人机接口    | 单色 LED           |
| `PA16`   | 运动控制    | 电机 2 方向 B      |
| `PA17`   | 已配置/保留 | OLED SPI SCLK      |
| `PA18`   | 已配置/保留 | OLED SPI MOSI/PICO |
| `PA19`   | 调试        | SWDIO              |
| `PA20`   | 调试        | SWCLK              |
| `PA21`   | 已配置/保留 | UART2 TX           |
| `PA22`   | 运动控制    | 编码器 2 B 相      |
| `PA24`   | 已配置/保留 | UART2 RX           |
| `PA25`   | 已配置/保留 | UART3 RX           |
| `PA26`   | 已配置/保留 | UART3 TX           |
| `PA27`   | 人机接口    | 蜂鸣器             |
| `PA28`   | 显示        | OLED I2C SDA       |
| `PA29`   | 传感器      | MPU6050 I2C SCL    |
| `PA30`   | 传感器      | MPU6050 I2C SDA    |
| `PA31`   | 显示        | OLED I2C SCL       |

### PORTB


| 端口引脚 | 分类         | 功能               |
| -------- | ------------ | ------------------ |
| `PB0`    | 通信         | UART0 TX           |
| `PB1`    | 通信         | UART0 RX           |
| `PB2`    | 运动控制     | 编码器 1 B 相      |
| `PB3`    | 运动控制     | 编码器 1 A 相      |
| `PB4`    | 运动控制     | 电机 1 PWM         |
| `PB5`    | 运动控制     | 电机 2 PWM         |
| `PB7`    | 传感器与采集 | 循迹地址选择 0     |
| `PB8`    | 人机接口     | 五向键下           |
| `PB9`    | 人机接口     | 五向键左           |
| `PB10`   | 人机接口     | 五向键右           |
| `PB11`   | 人机接口     | 五向键中           |
| `PB12`   | 人机接口     | 五向键上           |
| `PB14`   | 已配置/保留  | OLED SPI MISO/POCI |
| `PB15`   | 传感器与采集 | 循迹地址选择 1     |
| `PB16`   | 已配置/保留  | OLED 复位          |
| `PB17`   | 已配置/保留  | OLED 数据/命令     |
| `PB18`   | 传感器与采集 | ADC1 通道 5        |
| `PB21`   | 人机接口     | 三键 K1            |
| `PB22`   | 人机接口     | RGB 蓝灯           |
| `PB23`   | 人机接口     | 三键 K2            |
| `PB24`   | 人机接口     | 三键 K3            |
| `PB25`   | 传感器与采集 | 循迹地址选择 2     |
| `PB26`   | 人机接口     | RGB 红灯           |
| `PB27`   | 人机接口     | RGB 绿灯           |
